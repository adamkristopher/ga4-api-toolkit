# Google Analytics 4 & Search Console Service Account Credentials
#
# To set up:
# 1. Go to Google Cloud Console (https://console.cloud.google.com)
# 2. Create a project (or select existing)
# 3. Enable the following APIs:
#    - Google Analytics Data API
#    - Google Search Console API
#    - Indexing API
# 4. Create a Service Account (IAM & Admin > Service Accounts)
# 5. Create and download a JSON key for the service account
# 6. Copy the values from the JSON key file below
# 7. Add the service account email to:
#    - GA4 property (Admin > Property Access Management) with "Viewer" role
#    - Search Console property (Settings > Users and permissions) as a User

# Your GA4 Property ID (found in GA4 Admin > Property Settings)
GA4_PROPERTY_ID=123456789

# Service account email from the JSON key file
GA4_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Private key from the JSON key file (keep the quotes and \n characters)
GA4_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Search Console site URL (required for SEO and indexing functions)
# Format: https://your-domain.com (must match exactly as registered in Search Console)
SEARCH_CONSOLE_SITE_URL=https://your-domain.com
